// const authenticate = (req, res, next) => {
//     const { password } = req.body;
//     password === 654321 ? next() : res.status(401).json({ message: "Invalid Password" });
// };

// module.exports = authenticate;
